#Useful Linux Shortcuts
Remote Names: lhcweb, docs, webedu, pythonedu

git init : initialize a git repo
git remote add (name) (url)
git add .
git commit -m "message"
git push -u (name) (branch)

git remote -v : See the name of Remotes
